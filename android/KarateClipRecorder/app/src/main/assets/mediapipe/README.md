# MediaPipe Gesture Recognizer asset

Place the real MediaPipe Gesture Recognizer task bundle at:

`app/src/main/assets/mediapipe/gesture_recognizer.task`

The live Find Your Weapon analyzer intentionally fails with a readable missing-model error until this compatible `.task` file is added. Do not use a placeholder file.
